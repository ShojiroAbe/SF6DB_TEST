name: GitHub Actions

on:
  push:
    branches:
      - main
      - dev

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Run lint
        working-directory: frontend
        run: npm run lint
      
      - name: Echo
        run: echo "hello world!!"